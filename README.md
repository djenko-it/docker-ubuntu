# docker-ubuntu

## Installation de Docker sous Ubuntu WSL

```
 sudo apt-get update
```

```
sudo apt-get update
sudo apt-get install \
    ca-certificates \
    curl \
    gnupg \
    lsb-release
```